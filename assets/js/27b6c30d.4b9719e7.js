"use strict";(globalThis.webpackChunkwegent_docs=globalThis.webpackChunkwegent_docs||[]).push([[5568],{28453:(e,t,n)=>{n.d(t,{R:()=>i,x:()=>a});var s=n(96540);const o={},r=s.createContext(o);function i(e){const t=s.useContext(r);return s.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:i(e.components),s.createElement(r.Provider,{value:t},e.children)}},80355:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>l,contentTitle:()=>a,default:()=>d,frontMatter:()=>i,metadata:()=>s,toc:()=>h});const s=JSON.parse('{"id":"wegent/developer-guide/wework-message-scroll","title":"Wework Message Scroll Verification","description":"The Wework desktop chat list uses virtual scrolling. After sending a message, scrolling must cover the asynchronous layout measurements for both the user message and waiting indicator until the list is stably pinned to the bottom, keeping the thinking indicator fully visible. As a streaming response grows, the view should follow it only while the user remains at the bottom; otherwise the user\'s current viewport must be preserved.","source":"@site/docs/wegent/developer-guide/wework-message-scroll.md","sourceDirName":"wegent/developer-guide","slug":"/wegent/developer-guide/wework-message-scroll","permalink":"/wegent-docs/docs/wegent/developer-guide/wework-message-scroll","draft":false,"unlisted":false,"editUrl":"https://github.com/wecode-ai/wegent-docs/edit/main/docs/wegent/developer-guide/wework-message-scroll.md","tags":[],"version":"current","sidebarPosition":19,"frontMatter":{"sidebar_position":19},"sidebar":"wegentSidebar","previous":{"title":"Sandbox Workspace Archive","permalink":"/wegent-docs/docs/wegent/developer-guide/sandbox-workspace-archive"},"next":{"title":"External OAuth 2.0 Integration","permalink":"/wegent-docs/docs/wegent/developer-guide/external-oauth-integration"}}');var o=n(74848),r=n(28453);const i={sidebar_position:19},a="Wework Message Scroll Verification",l={},h=[{value:"Streaming Rendering Ownership",id:"streaming-rendering-ownership",level:2}];function c(e){const t={code:"code",h1:"h1",h2:"h2",header:"header",li:"li",p:"p",pre:"pre",ul:"ul",...(0,r.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"wework-message-scroll-verification",children:"Wework Message Scroll Verification"})}),"\n",(0,o.jsx)(t.p,{children:"The Wework desktop chat list uses virtual scrolling. After sending a message, scrolling must cover the asynchronous layout measurements for both the user message and waiting indicator until the list is stably pinned to the bottom, keeping the thinking indicator fully visible. As a streaming response grows, the view should follow it only while the user remains at the bottom; otherwise the user's current viewport must be preserved."}),"\n",(0,o.jsx)(t.p,{children:"Implementations must preserve these invariants:"}),"\n",(0,o.jsxs)(t.ul,{children:["\n",(0,o.jsx)(t.li,{children:"The final streaming message uses the virtualizer's normal measurement path, avoiding duplicate measurements that can invalidate the bottom anchor."}),"\n",(0,o.jsx)(t.li,{children:"A new user message or waiting indicator must not rely on one synchronous scroll. Follow layout-height changes through a short stabilization window, then restore the latest-turn reading-position protection."}),"\n",(0,o.jsx)(t.li,{children:"Once the user scrolls upward, later message-height changes must not pull the viewport back to an earlier message. The pause must be kept even when the upward scroll has not yet crossed the bottom tolerance; the follow engine must not yank the viewport back to the bottom on account of that."}),"\n",(0,o.jsxs)(t.li,{children:["The scroll anchor belongs to the app: the chat scroll container keeps ",(0,o.jsx)(t.code,{children:"overflow-anchor: none"})," on at all times, so the browser never adjusts ",(0,o.jsx)(t.code,{children:"scrollTop"})," behind the reader's back when content height changes. The browser's own anchoring reacts in opposite directions depending on whether the change happened above or below the viewport, and relying on that guess is what produced the repeated bounces and the viewport drifting down. With it disabled, only two things can move the reader: their own scrolling, and the layout change that just happened \u2014 both measurable."]}),"\n",(0,o.jsxs)(t.li,{children:['There is exactly one decision point, and it only relies on measurements: while the reader owns the viewport, the scroll area\'s layout-change handler takes "where the sampled text sits on screen now" minus "where it sat when sampled", adds "how far ',(0,o.jsx)(t.code,{children:"scrollTop"}),' moved in that window", and writes the result back to ',(0,o.jsx)(t.code,{children:"scrollTop"})," (",(0,o.jsx)(t.code,{children:"correction = \u0394anchorOffset + \u0394scrollTop"}),"). A pure reader scroll moves the text by exactly ",(0,o.jsx)(t.code,{children:"-\u0394scrollTop"}),", so the two cancel and their scrolling is never taken back; whatever is left is the layout change's own effect, which has to be given back. That single rule covers a whole-list reflow (the conversation narrowing when the file panel opens), a re-measured row above the viewport, and a streaming response growing below it, and it holds for every scroll origin (moving ",(0,o.jsx)(t.code,{children:"scrollTop"})," by ",(0,o.jsx)(t.code,{children:"x"})," moves the text by ",(0,o.jsx)(t.code,{children:"-x"})," in each). The handler runs inside the ResizeObserver callback (after layout, before paint), so the move is never visible; when the sampled text is no longer on screen the change cannot be attributed, so nothing is guessed and the sample is simply retaken. Two further properties have to hold: a browser clamp that pulls the offset into a shrunken range (the ",(0,o.jsx)(t.code,{children:"scrollTop"})," clamp when the content gets shorter) is the layout's own effect exactly like the height change, not the reader's scrolling, so it has to be attributed to the layout before the reader's scrolling is measured \u2014 otherwise every later correction keeps asking for an offset that no longer exists; and the sample may only be replaced once the correction actually landed, otherwise the window spans across it. A clamped write means this layout has not reached a usable range yet (the frame that measures a long answer as hundreds of thousands of pixels tall while the file panel closes is that case): keep the sample, count the part that was applied as our own write, and let the next layout change finish putting the text back instead of adopting the clamped position as the one the reader chose."]}),"\n",(0,o.jsx)(t.li,{children:"Layout height must never be fabricated to keep the scrollbar still (for example by holding a shrunken row at its old height): the fabricated height has to be released eventually, and the release moves the scrollbar and the content again while leaving blank space that never disappears once the reader scrolls to it. Scrollbar movement can only be reduced with better initial estimates, which shrink the re-measure deltas themselves."}),"\n",(0,o.jsxs)(t.li,{children:["The user viewport anchor is the reading position the user chose (message plus text offset plus the ",(0,o.jsx)(t.code,{children:"scrollTop"})," it belongs to). It is refreshed on real user intent (wheel, pointer, touch, key) and after every correction; when no visible anchor can be resolved the previous sample is kept, and an empty capture must never overwrite a usable anchor."]}),"\n",(0,o.jsx)(t.li,{children:"When the user explicitly clicks \u201cscroll to bottom,\u201d the button's pointer event must not remain recorded as manual scroll intent. The action must own bottom following through a short stabilization window so later virtual measurements or streaming growth still reach the latest bottom. The bottom-follow ownership must be held until the viewport actually settles at the bottom; it must not be released at a fixed point in time, otherwise a still-growing response can leave the viewport mid-conversation."}),"\n",(0,o.jsxs)(t.li,{children:["When reopening a long conversation, the canonical turn-level conversation view must merge by the global ",(0,o.jsx)(t.code,{children:"messageIndex"})," returned by the executor transcript. A locally preserved stopped turn that is absent from the paginated snapshot must return to its original position instead of being appended after the latest AI response; temporary turns without an index may use message timestamps as a secondary ordering key. Provider transcript pages remain Provider-owned: deduplicate them by stable IDs without merging local stopped turns into them or bypassing their pagination."]}),"\n",(0,o.jsxs)(t.li,{children:["When an idle task receives a new goal, the goal and first instruction must be sent atomically through one ",(0,o.jsx)(t.code,{children:"runtime.tasks.send"}),". Codex ",(0,o.jsx)(t.code,{children:"thread/goal/set"})," automatically starts the goal turn, so the executor must await that turn instead of issuing another ",(0,o.jsx)(t.code,{children:"turn/start"}),"; otherwise the real output is associated with the wrong position while the duplicate idle turn eventually shows a \u201cMessage generation failed\u201d error after 180 seconds."]}),"\n",(0,o.jsx)(t.li,{children:"Regression coverage must send a message in a long-history real Electron conversation, pause the model response, and assert that the scroller is at the bottom and the waiting indicator is fully contained by the scroll-container bounds."}),"\n"]}),"\n",(0,o.jsx)(t.h2,{id:"streaming-rendering-ownership",children:"Streaming Rendering Ownership"}),"\n",(0,o.jsx)(t.p,{children:"Streaming text and scroll following each have exactly one scheduler. Component animation, direct scrolling, and delayed scroll timers must not compete:"}),"\n",(0,o.jsxs)(t.ul,{children:["\n",(0,o.jsxs)(t.li,{children:[(0,o.jsx)(t.code,{children:"useBufferedStreamingText"})," only coalesces text updates received within one frame into the next animation frame. It must not maintain a typewriter queue that trails the authoritative message content; the next frame must commit the latest complete prefix, with a short timeout fallback when frames are unavailable."]}),"\n",(0,o.jsxs)(t.li,{children:[(0,o.jsx)(t.code,{children:"Streamdown"})," remains in ",(0,o.jsx)(t.code,{children:"streaming"})," mode for incremental Markdown parsing. Existing windowing and freezing continue to own non-tail Markdown blocks; animation must not rebuild the whole message tree."]}),"\n",(0,o.jsxs)(t.li,{children:["Streaming auto-follow in ",(0,o.jsx)(t.code,{children:"ScrollableMessageArea"})," is owned by one damped spring. ResizeObserver callbacks, message appends, and runtime-state changes may only ask that spring to keep tracking the latest bottom; they must not also perform streaming jumps or schedule stabilization timers."]}),"\n",(0,o.jsx)(t.li,{children:"While the spring owns bottom following, the virtualizer only measures message heights and converts bottom-origin coordinates; it must not directly rewrite list height or scroll position. Only after the user scrolls upward and pauses auto-follow may the virtualizer synchronously compensate for an offscreen streaming message growing to preserve the reading anchor."}),"\n",(0,o.jsx)(t.li,{children:"User upward scrolling, turn-navigation suspension, or auto-scroll suspension immediately cancels the spring. Non-streaming layout stabilization, history restoration, and explicit \u201cscroll to bottom\u201d actions keep their existing paths without running concurrently with the streaming spring."}),"\n",(0,o.jsxs)(t.li,{children:["With ",(0,o.jsx)(t.code,{children:"prefers-reduced-motion"}),", skip spring motion and position the viewport at the bottom immediately."]}),"\n"]}),"\n",(0,o.jsx)(t.p,{children:"Run the desktop regression:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{className:"language-bash",children:"cd wework\npnpm e2e:desktop:streaming-text\n"})}),"\n",(0,o.jsx)(t.p,{children:"The scenario uses real backend requests and a deterministic SSE response stream supplied by the desktop E2E harness. It pauses the server before output begins so the race between the user message rendering and the later waiting-indicator and virtual-list measurements is deterministic. Do not hide a scroll regression by skipping the scenario, mocking frontend requests, or weakening the bottom or visibility assertions."}),"\n",(0,o.jsx)(t.p,{children:"Streaming rendering changes must also verify that frame coalescing immediately displays the latest complete prefix, including Unicode content, plus spring convergence, user-scroll pause, and bottom-follow recovery. The real Electron scenario must retain viewport-anchor evidence before and after later streaming appends and verify that those appends do not move the user-selected anchor. The explicit bottom-recovery regression must dispatch real pointer-down and click events, continue appending enough content to change the virtualized height after the click, and finally assert that the remaining bottom distance is within the scroll tolerance."}),"\n",(0,o.jsx)(t.p,{children:"The ordering regression first stops a response, sends 26 follow-up turns to move the stopped turn outside the default transcript page, switches to other tasks, reopens the original task, and verifies that the latest AI response\u2014not the \u201cStopped\u201d notice\u2014occupies the bottom position."}),"\n",(0,o.jsxs)(t.p,{children:["The goal-continuation regression verifies that one ",(0,o.jsx)(t.code,{children:"runtime.tasks.send"})," payload contains both the goal and first instruction, then reads the real executor log to confirm that submission awaits exactly one turn automatically started by ",(0,o.jsx)(t.code,{children:"thread/goal/set"}),", issues no extra ",(0,o.jsx)(t.code,{children:"turn/start"}),", and renders no error card during streaming or after completion."]})]})}function d(e={}){const{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(c,{...e})}):c(e)}}}]);